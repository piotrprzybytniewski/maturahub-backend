nelmio_api_doc:
    documentation:
        info:
            title: Final exam application
            description: This is an awesome app to prepare before final exam (Polish matura)
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
    models: { use_jms: false }